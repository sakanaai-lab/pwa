# このリポジトリで作業するときの手引き

Aquarium Chat のコードベース。AIエージェント（Claude Code / Codex など）が作業するとき向けの手順をまとめる。

このリポジトリには**エラーにならない失敗**がいくつかある。ビルドを忘れる、キャッシュ版を上げ忘れる、料金表の行を置く位置を間違える。どれも壊れた感じがしないまま間違った結果が出るので、下の手順をそのまま踏むこと。

## 変更のたびに踏む手順

```bash
npm ci          # node_modules が無いときだけ
# src/ を編集
npm run build   # app.js を作り直す（必須）
npx vitest run  # 全部通ること
```

そのうえで `sw.js` と `index.html` のキャッシュ版を上げる（後述）。

コミットには **`app.js` も含める**こと。

## 1. `app.js` はビルド生成物だが、コミットする

`src/main.js` を入口に esbuild で `app.js` を作っている（`build.mjs`）。GitHub Pages がそのまま配信するので、`app.js` もリポジトリに入っている。

- **`src/` を編集しただけではアプリは変わらない。** `npm run build` を走らせること。
- **`app.js` を直接編集してはいけない。** 次のビルドで消える。
- ビルドを忘れると、`src/` と `app.js` が食い違ったまま push される。テストは `src/` を見るので**テストは通ってしまう**。気づけるのは実際にアプリを開いたときだけ。

ビルドを実行できない環境なら、`src/` を編集しないこと。中途半端な状態で push するより、実行できる環境に回したほうがいい。

## 2. キャッシュ版を上げる

Service Worker がキャッシュするので、版を上げないと既存ユーザーには古いアプリが出続ける。2か所を同じ値に揃える。

- `sw.js` の `CACHE_NAME`（例: `gemini-pwa-cache-v1.25.84`）
- `index.html` の `<script src="app.js?v=1.25.84">`

`style.css` を変更したときは `index.html` の `style.css?v=` も上げる。**変更していないなら触らない**（無駄なキャッシュ破棄になる）。CSS を変えない変更のほうが多いので、この2つの値は普段ずれている。

版番号はパッチ部分を1つ増やすだけでいい。`src/constants.js` の `APP_VERSION`（`'1.25'`）とは別物なので、そちらは動かさない。

## 3. 料金表（`src/utils/pricing.js`）

`MODEL_PRICING` はオブジェクトのキーを**上から順に前方一致**（`startsWith`）で照合する。**長い・具体的なキーを、短いキーより先に置くこと。**

```js
'claude-opus-5-5': { ... },  // これが先
'claude-opus-5':   { ... },  // これが後
```

逆にすると `claude-opus-5-5` が `claude-opus-5` に先に当たり、**違う単価で静かに計算される**。過去に Sonnet 5 / Fable 5.1 / deepseek-flash / Opus 5.5 で繰り返し踏んでいる。

モデル名は `normalizeModelName()` で正規化されてから引かれる。小文字化し、`/` より前のベンダー接頭辞と `:variant` を落とし、`数字.数字` を `数字-数字` にする（`gpt-5.6-sol` → `gpt-5-6-sol`）。**テーブルのキーはこの正規化後の形で書く。**

単位は全て USD / 100万トークン。`in`=入力（キャッシュミス）、`out`=出力、`cw5m`/`cw1h`=キャッシュ書き込み、`cr`=キャッシュ読み込み（ヒット）。

### 値段が変わったとき

過去のメッセージの推定コストを変えてはいけない。「変更前の単価」を別テーブルに残し、切り替え時刻の定数と一緒に `getPricing()` で日時から選ぶ。既存の `MODEL_PRICING_BEFORE_*` / `*_PROMO` がその形なので真似する。

### 推測で埋めない

公表されていない値（長コンテキストの閾値など）は入れない。入れないことをコメントで明示し、テストで固定する。

## 4. モデル一覧（`src/constants.js`）

`ANTHROPIC_MODELS` などの配列がプロバイダー別の選択肢。**一覧に足したら料金表にも足す。** 料金が無いモデルは ⓘ の推定コストが出なくなる。`tests/model-lists.test.js` がこれを見張っている。

`RETIRED_MODEL_MAP` は提供終了したモデルから後継への転送。提供終了に気づいたらここに足す。

## 5. 更新履歴（`src/constants.js` の `VERSION_HISTORY`）

ユーザーに見せる更新内容。キーは `'1.62'` のような文字列で、**大きいほうが新しい**（数値としてではなくセグメントごとに比較するので `1.58 > 1.9`）。新しい項目は先頭に足す。

更新後にブラウザで出る通知は**最新1件だけ**。3件だと画面がいっぱいになるので意図的にそうしている（`src/app-logic/lifecycle.js`）。残りは件数だけ伝えて、全件は設定の「更新履歴」から読める。

文面はユーザー向け。何が変わって何が嬉しいのかを書く。内部の関数名は出さない。

**`README.md` の「更新履歴（主要変更のみ）」にも追記すること。** ここは忘れやすく、実際に1か月ぶん（13件）抜けたことがある。ユーザーに関係のない内部変更は省いてよいが、機能追加・料金の変更・目に見える不具合修正は載せる。あわせて、プロバイダーを足したときや推定コストの対応範囲が変わったときは、README 冒頭の「本家からの主な変更点」と「APIキーの設定」の表も古くなっていないか見ること。

## 6. テスト

`tests/` に vitest + jsdom。**純粋関数は `src/utils/*.js` に切り出して単体テストを書く**のがこのリポジトリの流儀。DOM や IndexedDB に触る関数はテストしづらいので、ロジックだけ引き剥がす。

バグを直したときは、そのバグを再現する回帰テストを足す。既存のテストに「回帰:」で始まるコメントが付いているのがそれ。

テストが落ちたとき、**テストの前提のほうが古い**ことがある（割引が終わった、「知らないモデル」として使っていた名前が実在するようになった、など）。実装とテストのどちらが間違っているか確かめてから直すこと。テストを消して通すのは無し。

## 7. コミットとプッシュ

- コミットメッセージは日本語。何をなぜ変えたかを書く。
- 作業ブランチを切って PR を出し、squash merge で `main` に入れる。`main` に直接 push しない。
- PR がマージされたら、作業ブランチは `origin/main` から切り直す（マージ済みの履歴の上に積まない）。

APIキーの類はリポジトリに持たない（設定画面からユーザーが入力する方式）。

## ディレクトリ構成

```
src/
  main.js          エントリポイント（ここから app.js がビルドされる）
  constants.js     モデル一覧・既定値・更新履歴
  api.js           プロバイダーごとのリクエスト組み立てと分岐
  state.js         アプリ全体の状態
  db.js            IndexedDB
  ui.js            描画まわり
  app-logic/       画面ごとの処理（chat, message, lifecycle, memory, sync ほか）
  utils/           純粋関数（pricing, usage, format, model-select, tts ほか）
tests/             vitest
app.js             ビルド生成物（編集しない・コミットする）
sw.js              Service Worker（CACHE_NAME を上げる）
index.html         app.js?v= と style.css?v= を上げる
build.mjs          esbuild 設定
```
