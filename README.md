# Aquarium Chat（sakanaai-lab カスタマイズ版）

> [!NOTE]
> これは [kinkan04/Gemini-PWA-Mk-II](https://github.com/kinkan04/Gemini-PWA-Mk-II)（本家：[ona-oni/geminipwa](https://github.com/ona-oni/geminipwa/)）を個人用にフォーク・改造したものです。

> [!TIP]
> 💬 **[チャットを開く →](https://sakanaai-lab.github.io/pwa/#chat)**

### 🔧 本家からの主な変更点

| 項目 | 内容 |
|------|------|
| **アプリ名** | 「Aquarium Chat」に改名 |
| **カラーテーマ** | ライトモードを水色・ブルーグリーン系「Aquarium」テーマに変更 |
| **マルチプロバイダー対応** | OpenAI・Anthropic・Groq・DeepSeek・xAI・Mistral・Sakana(fugu) のAPIをネイティブサポート。Anthropic使用時はプロンプトキャッシングを自動適用（システムプロンプト＋会話履歴を設定TTLでキャッシュ、大幅なコスト削減） |
| **カスタムモデル登録** | 各プロバイダーに任意のモデルIDを追加可能 |
| **推論モデル対応** | OpenAI o-series / DeepSeek-R1 / Anthropic Adaptive Thinking に最適化されたAPIパラメータを自動適用 |
| **思考プロセス表示** | Gemini・Claude・DeepSeek-R1 の思考プロセスを折りたたみで表示、日本語翻訳も対応 |
| **Claude思考の深さ制御** | Anthropic APIの思考の深さ（effort: low/medium/high/max）を設定画面から選択可能 |
| **設定画面UI** | アコーディオン形式に再構築、保存ボタンをヘッダー右上に固定 |
| **NovelAI画像生成** | Function Calling経由でアニメ・イラスト調の画像を生成。「絵を描いて」と言うだけで自動呼び出し。Gemini・OpenAI・Claude の全プロバイダーで動作 |
| **チャットタイトル自動生成** | 初回やり取り完了後に AI がタイトルを自動生成（消費トークンは数百程度） |
| **会話統計表示** | フローティングパネルの ⓘ ボタンからメッセージ数・トークン内訳・キャッシュヒット率・推定コスト（Anthropicモデルのみ）を表示 |
| **Dropbox同期の競合処理** | 競合時の確認ダイアログを廃止し、自動マージに変更 |
| **Stable Diffusion連携** | 個人環境では不使用のため削除 |

---

# 目次

- [はじめて使う方へ](#はじめて使う方へ)
- [APIキーの設定](#APIキーの設定)
- [カスタムモデルの追加方法](#カスタムモデルの追加方法)
- [思考プロセスを表示する](#思考プロセスを表示する)
- [アプリの更新方法](#アプリの更新方法)
- [セキュリティについて](#セキュリティについて)
- [よくあるトラブルQ&A](#よくあるトラブルqa)
- [主な機能](#主な機能)
- [システムプロンプトの使い方](#システムプロンプトの使い方)
- [プロジェクト機能](#プロジェクト機能)
- [プロファイル機能](#プロファイル機能)
- [Dropbox連携](#Dropbox連携)
- [会話を画像として保存する](#会話を画像として保存する)
- [NovelAI画像生成の設定方法](#novelai画像生成の設定方法)
- [便利なリンク集](#便利なリンク集)
- [更新履歴](#更新履歴主要変更のみ)

---

# はじめて使う方へ

1. **[アプリにアクセス](https://sakanaai-lab.github.io/pwa/)** してPWAを開きます。
2. 画面右上の **歯車アイコン（設定）** を開きます。
3. 使いたいAIのAPIキーを入力して **「設定を保存」** を押します。
4. **「🔄 全プロバイダーの最新モデルを取得」** ボタンを押すと、設定したAPIキーに対応する最新モデルが自動で一覧に追加されます。
5. モデル選択ドロップダウンから使いたいモデルを選びます。
6. チャット画面に戻って会話を開始します。

> **💡 ヒント：** スマートフォンでホーム画面に追加（PWAインストール）するとアプリのように使えます。

---

# APIキーの設定

設定画面の **「プロバイダー」** セクションで、使いたいサービスを選んでAPIキーを入力してください。

| プロバイダー | 取得先 | 設定画面での名称 |
|---|---|---|
| **Gemini**（Google） | [Google AI Studio](https://aistudio.google.com/app/apikey) | Gemini |
| **ChatGPT**（OpenAI） | [OpenAI Platform](https://platform.openai.com/api-keys) | OpenAI |
| **Claude**（Anthropic） | [Anthropic Console](https://console.anthropic.com/) | Anthropic |
| **Groq**（高速推論） | [Groq Console](https://console.groq.com/keys) | Groq |
| **DeepSeek** | [DeepSeek Platform](https://platform.deepseek.com/) | DeepSeek |
| **Grok**（xAI） | [xAI Console](https://console.x.ai/) | xAI |
| **Mistral** | [Mistral Console](https://console.mistral.ai/) | Mistral |
| **ZAI** | ZAI管理画面 | ZAI |
| **OpenRouter** | [OpenRouter](https://openrouter.ai/keys) | OpenRouter |

> **⚠️ 注意：** APIキーはお使いのブラウザのIndexedDBにのみ保存されます。PCとスマホでは別々のデータベースになるため、スマホで使う場合はスマホ側でも設定が必要です。

---

# カスタムモデルの追加方法

標準リストにないモデル（新モデル・ベータモデルなど）を追加できます。

## 自動で追加する（推奨）

設定画面の **「🔄 全プロバイダーの最新モデルを取得」** ボタンを押すと、APIキーが設定済みの全プロバイダーから最新モデルを自動取得してドロップダウンに追加します。

> 取得したモデルは「API取得モデル」グループに表示されます。次回取得時に廃止されたモデルは自動で消えます。

## 手動で追加する

特定のモデルIDを直接指定したい場合：

1. 設定画面の **「カスタムモデル」** セクションを開きます。
2. 追加したいプロバイダーのテキストエリアに、**モデルIDをカンマ区切り** で入力します。

```
例（OpenAI）: gpt-5.1-2025-11-13, o4-mini-high
例（Gemini）: gemini-3.1-pro-preview, gemini-2.5-flash-exp
```

3. **「設定を保存」** を押します。
4. モデル選択ドロップダウンの **「追加モデル」** グループに表示されます。手動追加モデルは自動取得で削除されません。

---

# 思考プロセスを表示する

対応モデルで思考（推論）プロセスを本文の上に折りたたみ表示できます。

## 対応モデル

| モデル | 思考の仕組み |
|---|---|
| Gemini 2.5 Pro / Gemini 3.x | Googleの思考機能（thinkingConfig） |
| Claude Opus 4.6 / 4.7 / Sonnet 4.6 | Anthropic Adaptive Thinking |
| DeepSeek-R1 / deepseek-reasoner | reasoning_content フィールド |
| Groqのr1系モデルなど | `<think>` タグ |

## 設定方法

### Gemini / DeepSeek / Groq の場合

1. 設定画面の **「その他パラメータ」** セクションを開きます。
2. **「Thinking Budget」** に数値を入力します（推奨：`1024`）。
   - `0` を入力すると明示的にオフになります。
   - Gemini の場合、1〜1024 は 1024 固定になります。
   - ⚠️ 数値を大きくしすぎると、思考プロセスが長くなる分だけモデルが内容を深く検討するため、ロールプレイなどの際にコンテンツポリシーで応答を断られやすくなります。**1024 推奨**。
3. **「Include Thoughts」** にチェックを入れます。
4. **「設定を保存」** します。

### Claude（Anthropic）の場合

1. 設定画面でプロバイダーを **Anthropic** に設定します。
2. Anthropic設定セクションの **「思考の深さ (Effort)」** ドロップダウンから深さを選択します。
   - **OFF**：思考なし（応答の詳しさにも影響）
   - **low**：速度・コスト重視
   - **medium**：バランス型（Sonnet 4.6推奨）
   - **high**：デフォルト（パラメータ省略時と同等）
   - **max**：最大品質
3. **「Include Thoughts」** にチェックを入れると、思考プロセスが折りたたみ表示されます。
4. **「設定を保存」** します。

> **💡 ポイント：** Claude の思考の深さ（effort）は adaptive thinking を使用しており、従来の `budget_tokens` 指定に比べてモデルが自律的に思考量を調整します。Thinking Budget を空欄にしたまま effort だけ設定すれば OK です。

> **💡 ヒント：** 「思考プロセスを日本語に翻訳する」にチェックを入れると、英語の思考プロセスを自動で日本語に翻訳します。翻訳には別途 Gemini API キーが必要です。

> **⚠️ 注意：** OpenAI の o1/o3/o4-mini はAPIが思考内容を公開していないため、表示できません。

---

# アプリの更新方法

新しい機能やバグ修正が反映されない場合は、以下の手順でキャッシュを更新してください。

**方法1（推奨）：** 設定画面の **画面最下部の「アプリを更新（キャッシュクリア）」** ボタンを押す。

**方法2：** `Ctrl + Shift + R`（Mac は `Cmd + Shift + R`）で強制リフレッシュ。

**方法3（スマホ）：** Safariの場合、設定 → Safari → 詳細 → Webサイトデータ からサイトのデータを削除してリロード。

---

# セキュリティについて

「APIキーや会話内容は安全なの？」と気になる方向けのまとめです。

## APIキーの保存場所

APIキーはお使いの**ブラウザのIndexedDB（端末内）にのみ保存**されます。サーバーには送られません。

| 項目 | 内容 |
|---|---|
| 保存場所 | 端末内のブラウザ（IndexedDB） |
| 外部送信 | なし（APIキーはAI各社のサーバーにのみ直接送信） |
| 他のユーザーから見える？ | 見えない（端末ごとに独立） |
| PCとスマホで共有される？ | されない（別々に設定が必要） |

## 会話データの行き先

送信したメッセージは**選択中のAIプロバイダー（Google・OpenAI・Anthropicなど）のサーバーに直接送信**されます。このPWA自体のサーバーは存在しないため、会話内容がここを経由することはありません。

## Dropbox連携について

- 各ユーザーが**自分のDropboxアカウント**で認証します
- チャット履歴は自分のDropboxにのみ保存され、他のユーザーからは見えません
- このアプリがDropboxに保存できるのは専用のアプリフォルダのみです（App folder権限）

## 身内に共有するときの注意点

- **APIキーは絶対に教えない** → 料金が一緒に請求されます
- 各自で自分のAPIキーを取得・設定してもらう
- Geminiは無料枠があるので、試したい人はまずGeminiがおすすめ

---

# よくあるトラブルQ&A

## 思考プロセスが表示されない

**確認ポイント：**
1. 設定 →「その他パラメータ」→「Thinking Budget」に `1024` が入っているか
2. 「Include Thoughts」にチェックが入っているか
3. 設定を保存したか
4. 対応モデルを使っているか（Gemini 2.5/3系、Claude Opus 4以降、DeepSeek-R1など）

> OpenAI の o1/o3/o4-mini はAPIが思考内容を非公開のため表示できません。

## モデルが切り替わらない / モデルが見つからない

- 「🔄 全プロバイダーの最新モデルを取得」ボタンを押すと最新一覧を取得できます
- 使いたいモデルが一覧にない場合は「追加モデル」欄にモデルIDを直接入力してください

## Dropbox連携で「Invalid redirect_uri」エラーが出る

[Dropbox App Console](https://www.dropbox.com/developers/apps) を開き、Redirect URIs に以下が登録されているか確認してください：
```
https://sakanaai-lab.github.io/pwa/
```

## 新しい機能が反映されない

サービスワーカーのキャッシュが古い可能性があります。設定画面の「アプリを更新（キャッシュクリア）」ボタンを押してください。

## スマホで設定したのにPCで反映されない（またはその逆）

端末ごとにデータが独立しているため、それぞれの端末で設定が必要です。Dropbox連携を使うとデータを同期できます。

## Function Calling（関数）ってどう使うの？

設定で **「Function Callingを使用する」** をオンにするだけで、あとは **普通に会話するだけ** です。AIが必要に応じて自動で関数を呼び出します。

**例：**
- 「今日って何曜日？」→ AIが `getCurrentDateTime` を自動実行して日時を取得
- 「1d100でダイスを振って」→ AIが `rollDice` を自動実行
- 「私の名前はたけしだよ、覚えておいて」→ AIが `manage_persistent_memory` で記憶を保存

明示的に呼ばせたい場合は、会話中に関数名を書くと確実に実行されます：
```
「getCurrentDateTimeを使って今の日時を確認して」
```

> **注意：** Function Callingを使うと「モデル呼び出し → 関数実行 → モデル呼び出し」と処理されるため、1回の生成で **APIリクエストが複数回消費されます**。

## 履歴一覧を見たら、チャットが消えた／少ししか表示されない

プロジェクトフィルターが有効になっている可能性があります。

**ヘッダーのプロジェクト選択ドロップダウンを「すべてのプロジェクト」に切り替えてください。** 全チャットが表示されるはずです。

プロジェクトを選択している状態では、そのプロジェクトに属するチャットのみが履歴一覧に表示されます（データが消えたわけではありません）。また、プロジェクト選択中に作成した新規チャットは自動的にそのプロジェクトに紐づいて保存されます。

| 状態 | 履歴一覧に表示されるチャット |
|---|---|
| 「すべてのプロジェクト」選択中 | 全チャット |
| プロジェクト名を選択中 | そのプロジェクトのチャットのみ |

## プロジェクトとプロファイルの違いは？

用途が異なる2つの機能です。

| | プロジェクト | プロファイル |
|---|---|---|
| **目的** | チャットのテーマ・世界観を管理 | ユーザー自身の設定を管理 |
| **主な設定** | システムプロンプト、ナレッジファイル、デフォルトモデル | AIパラメータ、UIテーマ、メモリ設定など |
| **切り替え** | チャットごとに選択 | アカウントのように切り替え |
| **使い分けの例** | 「翻訳用」「小説執筆用」「コーディング用」など用途別に作成 | 「本番用」「テスト用」など環境ごとに分けたい場合 |

**シンプルに使う場合**はプロジェクトだけ活用すれば十分です。プロファイルは複数人で使ったり、設定を丸ごと切り替えたい上級者向けの機能です。

> **ℹ️ プロジェクトとナレッジファイルはDropbox同期の対象です。** Dropbox連携を設定していれば、端末間で引き継ぎできます。

---

# 主な機能

## 💬 基本チャット機能
- マルチターン会話（会話履歴を保持）
- メッセージの編集・削除・再生成
- チャット履歴の保存（IndexedDB）・エクスポート/インポート
- セッション管理（複数チャットの切り替え）

## 🖼️ マルチモーダル
- 画像を添付してAIと会話（Gemini/OpenAI/Anthropic対応）
- AIによる画像生成（`generate_image` 関数、Gemini APIのみ）
- AIによる画像編集（`edit_image` 関数）
- AIによる動画生成（`generate_video` 関数、Gemini APIのみ）
- チャット背景の動的変更（`set_background_image` 関数）

## 🔄 コンテキスト管理
- **コンテキスト圧縮（要約）機能**：長い会話履歴を要約してAPIに送信するトークン量を削減
  - チャット左上のフローティングボタンから呼び出し（50メッセージ以上で有効化）
- **自動リトライ**：APIエラー時に自動で再試行（回数・間隔を設定可能）
- **タイムアウト設定**：応答が遅い場合に自動キャンセル

## 📊 会話統計
フローティングパネル（画面右下）の **ⓘ ボタン** から現在の会話の統計を確認できます。

| 項目 | 内容 |
|---|---|
| 合計メッセージ数 | 非表示メッセージを除いた件数 |
| 使用済みトークン合計 | 入力・出力の内訳付き |
| キャッシュ読み込み | 読み込みトークン数とヒット率（%） |
| キャッシュ書き込み | 書き込みトークン数 |
| 推定コスト | Anthropicモデル使用時のみ表示（Opus/Sonnet/Haikuを自動判別） |
| 会話サイズ | データのKBサイズ |
| 使用モデル | この会話で使用されたモデル名 |

> Gemini・OpenAI 等のプロバイダーではコスト推定は表示されません。

## ✍️ 文章支援
- **校正機能**：AIが送信前にメッセージの誤字・表現を修正
- **メモリ機能**：ユーザーの好みや傾向をAIが自動学習（ChatGPTのメモリ機能と同様）

## 🎨 UI カスタマイズ
- ダークモード / ライトモード切り替え
- ワイドモード（PC向け）
- チャット背景画像の設定
- バブルの透明度調整
- メッセージのターン番号・モデル名・送信時刻をラベル表示

## 🖼️ 画像保存
- 会話の好きな範囲を選んで1枚のPNG画像として保存できます。高コントラスト（白背景＋黒文字）で読みやすく、iPhone(Safari)にも対応。
- 詳しい手順は [会話を画像として保存する](#会話を画像として保存する) を参照してください。

---

# システムプロンプトの使い方

AIへの基本的な指示（役割・口調・ルールなど）をシステムプロンプトとして設定できます。設定できる場所が2つあります。

## 設定の「システムプロンプト（デフォルト）」

- 設定画面の **「その他パラメータ」** セクションにあります。
- **新規チャットを作成したとき** にこの内容が初期値として適用されます。
- すべてのチャットに共通の指示（「常に日本語で答えて」など）を入れておくのに便利です。

## チャットごとのシスプロ（チャット内編集）

- チャット画面の **システムプロンプト欄** から、そのチャット専用のシスプロを設定・編集できます。
- 一度チャット内で編集すると、そのチャットは設定のデフォルトとは独立した専用のシスプロを持ちます。

## 優先順位・干渉について

| 状況 | 使われるシスプロ |
|---|---|
| 新規チャット作成時 | 設定のデフォルト（初期値として適用） |
| 既存チャットを開いたとき | そのチャット専用のシスプロ（あれば優先） |
| チャット内で編集した場合 | 編集した内容が以降ずっと使われる |

> **ポイント：** 2つが同時に混合・結合されることはありません。チャット内で一度でも編集すると、設定のデフォルトは参照されなくなります。チャット内のシスプロを空にした場合も、設定のデフォルトに戻るのではなく「空のシスプロ」として扱われます。

---

# プロジェクト機能

チャット履歴をプロジェクトごとに整理し、プロジェクト専用のシステムプロンプト・モデル・ナレッジファイルをまとめて管理できます。

## 基本的な使い方

1. 画面上部のプルダウンから **「プロジェクト管理...」** を選びます。
2. プロジェクト名・システムプロンプト・デフォルトモデルを入力して **「プロジェクトを作成」** を押します。
3. プルダウンからプロジェクトを選択すると、そのプロジェクトのチャット履歴だけが表示されます。
4. **新規チャット（＋ボタン）** を押すと、プロジェクトのシスプロとモデルが自動で適用されます。

## ナレッジファイルの追加

プロジェクトにファイルを添付しておくと、そのプロジェクトの全チャットで**毎回自動的にAIに読み込まれます**。毎回手動で添付する手間がなくなります。

### 追加方法

1. **「プロジェクト管理...」** を開きます。
2. 作成済みプロジェクトの一覧を下にスクロールします。
3. 対象プロジェクトの **📎ボタン** を押してファイルを選択します（複数可）。

> **注意：** ナレッジは**プロジェクト作成後**に追加します。新規作成フォームからは追加できません。

### 対応ファイル形式

| 種類 | 拡張子 |
|---|---|
| テキスト | .txt / .md / .csv / .log |
| コード | .js / .ts / .py / .rb / .go / .java / .html / .css など |
| データ | .json / .yaml / .xml / .toml |
| **PDF** | .pdf（テキスト埋め込みPDFのみ対応。スキャンPDFは不可） |

### 仕組み

- ファイルの内容はIndexedDBにテキストとして保存されます。
- API送信時にシステムプロンプトの末尾に自動追記されます（チャット画面のシスプロ欄には表示されません）。
- ファイルが大きすぎるとAPIのコンテキスト上限を超える場合があります。目安は合計数万字以内。

---

# プロファイル機能

APIキー・モデル・システムプロンプト・各種パラメータをセットで「プロファイル」として保存し、ワンタップで切り替えられます。

**用途の例：**
| プロファイル名 | 設定例 |
|---|---|
| コーディング | モデル: `gpt-5.1` / システムプロンプト: `あなたは熟練のプログラマーです` |
| ロールプレイ | モデル: `claude-opus-4-6` / Extended Thinking有効 / キャラクター設定済み |
| 翻訳 | モデル: `gemini-flash-latest` / システムプロンプト: `翻訳専用` |

### 使い方
1. 設定画面の **「プロファイル」** セクションから「新規プロファイルを追加」。
2. プロファイル名を入力し、設定を行って保存。
3. 設定画面右上のプロファイル選択ドロップダウンで切り替え。

---

# Dropbox連携

チャット履歴・プロファイル・画像アセット・メモリ情報などPWA内の全データをDropboxにバックアップし、複数端末間で同期できます。

### 使い方
1. 設定画面の **「データ同期」** セクションにある **「Dropboxと連携する」** を押します。
2. Dropboxのアカウントでログインしてアクセスを許可します。
3. 同期頻度（即時・5/10/30メッセージごと・手動）を選択します。
4. **「今すぐ同期」** で手動同期も可能です。

### 注意点
- 複数端末から同期する場合は、最初に1台の端末でデータをまとめてから同期することを推奨します。
- 競合が発生した場合は両端末のデータを自動マージします。手動でクラウドから復元したい場合は「クラウドから復元」ボタンを使用してください。

---

# 会話を画像として保存する

チャットのやり取りを、選んだ範囲だけ1枚の画像（PNG）として保存できます。気に入ったシーンの保存や共有に便利です。

### 手順

1. チャット画面右側の**フローティングメニュー**にある画像保存アイコン（<span title="photo_library">🖼️ 重なった写真のマーク</span>。ホバーすると「**範囲を選んで画像保存**」と表示）を押します。
2. 画面下に確認バーが表示され、選択モードに入ります（「開始メッセージをタップ」と案内が出ます）。
3. **保存したい範囲の最初のメッセージ**をタップします（＝始点）。
4. 続けて**範囲の最後のメッセージ**をタップします（＝終点）。始点から終点までがハイライトされ、終点は何度でもタップし直して伸縮できます。
5. 確認バーの「**保存**」を押すと、選択範囲がまとめて画像化され、端末にダウンロードされます。
   - やめたいときは「**キャンセル**」を押すと選択モードを抜けます。

### 補足

- 保存されるファイル名は `Aquarium_Chat_range_（日時）.png` 形式です。
- 選択範囲が非常に長い場合は、複数枚の画像に自動分割して保存されます。
- 始点と終点に同じメッセージを選べば、1メッセージだけを画像化することもできます。

---

# NovelAI画像生成の設定方法

ロールプレイ中に「絵を描いて」と言うと、AIが自動でNovelAI APIを呼び出してアニメ・イラスト調の画像を生成します。

**必要なもの**: [NovelAI](https://novelai.net/) のサブスクリプション（Tablet プラン以上）

**設定手順**:
1. [novelai.net](https://novelai.net/) にログインし、左サイドバー一番下のユーザーアイコン →「**Account**」→「**Get Persistent API Token**」でAPIトークンを発行
2. PWAの設定画面「**NovelAI設定**」セクションにトークンを貼り付け、モデルを選択して保存
3. 「**Function Callingを使用する**」をオンにする

設定後、Function Callingが有効なチャットで「今のシーンの絵を描いて」のように指示すると画像が生成されます。

| モデル | 特徴 |
|---|---|
| **V4.5 Curated**（推奨） | 高品質・安定。デフォルト選択 |
| **V4.5 Full** | 制限が少ない |
| **V3** | 旧モデル |

> **注意**: Claude（Anthropic）プロバイダーはコンテンツポリシーにより、会話内容によっては画像生成を拒否する場合があります。Gemini または OpenAI プロバイダーの使用を推奨します。

---

# 便利なリンク集

## API使用量・料金の確認

各プロバイダーの実際の使用量・請求額を確認できるページです。会話統計(ⓘ)の**推定コスト**はあくまで目安なので、正確な金額は各社のダッシュボードで確認してください。

| サービス | ページ |
|---|---|
| **OpenAI (ChatGPT)** | [使用量](https://platform.openai.com/usage) |
| **Anthropic (Claude)** | [使用量](https://console.anthropic.com/settings/usage) |
| **Google (Gemini)** | [使用量](https://aistudio.google.com/usage) |
| **OpenRouter** | [使用状況](https://openrouter.ai/activity) |
| **DeepSeek** | [使用量](https://platform.deepseek.com/usage) |

> **💡 ヒント：** チャット画面右側のフローティングメニューの **ⓘ（会話の統計）** を開くと、ダイアログ下部から各社の使用量ページへ直接ジャンプできます。

---

# 更新履歴（主要変更のみ）

| 日付 | 内容 |
|---|---|
| **2026-07-24** | **提供終了モデルの実行時サルベージ**：送信や要約の途中でモデルが提供終了していても「失敗」で終わらせず、後継モデルへの切替を案内します。既知の廃止（gemini-3-pro-preview 等）は後継へ自動切替、それ以外は現行モデルを提案して確認（勝手に別モデルへ切替えて想定外の課金にはしません）。要約は自動で再試行します |
| **2026-07-21** | **要約・メモリ自動学習を全プロバイダー対応**：これまで Gemini（要約は＋DeepSeek）専用で、Claude 等に切り替えると要約が失敗していた問題を解消。選択中のプロバイダー（Anthropic / OpenAI / Groq / xAI / Mistral / OpenRouter / Sakana 等）で動作するようになり、Gemini キーなしでも Claude だけで完結できます |
| **2026-07-21** | **提供終了モデルの自動移行**：Googleが提供終了した `gemini-3-pro-preview` を後継の `gemini-3.1-pro-preview` へ自動で移行。設定・要約モデルにこのモデルが残っていると「要約の生成に失敗しました（no longer available）」が発生していた問題を修正。モデル一覧からも削除しました |
| **2026-07-15** | **モデル取得エラーの理由を表示**：「全プロバイダーの最新モデルを取得」で失敗したとき、「HTTP 400」だけでなくAPIが返すエラー理由（「APIキーが無効です」等）をあわせて表示するように |
| **2026-07-15** | **OpenAIモデル一覧をテキストモデルのみにフィルタ**：「全プロバイダーの最新モデルを取得」で、ドロップダウンが `gpt-image-*` / `gpt-realtime-*` / `gpt-audio-*` や `gpt-4o-audio-preview` 等の音声・画像系で溢れるのを修正。`gpt-数字`（gpt-4o, gpt-5 等）・o系・chatgpt系のテキストモデルのみ表示 |
| **2026-07-13** | **名前マスキング（画像保存用）を追加**：設定で「本名,別名」を登録しておくと、会話を**画像保存・コピー**するときだけ名前を別名に置き換えます。画面表示・API送信・保存データは元のまま。SNS共有前に本名を伏せたいときに便利です（区切りは , 、 → が使用可、長い名前を優先置換） |
| **2026-07-06** | **会話統計に「API使用量」リンクを追加**：会話統計(ⓘ)ダイアログ下部から OpenAI / Claude / Gemini / OpenRouter / DeepSeek の各使用量ページへワンタップで移動できるように。READMEの「[便利なリンク集](#便利なリンク集)」も復活・5社に拡張しました |
| **2026-06-29** | **DeepSeek の時間帯料金に対応**：会話統計(ⓘ)の推定コストで、ピーク時間帯（日本時間 10:00〜13:00 / 15:00〜19:00、UTC 01:00-04:00 / 06:00-10:00）のメッセージは通常価格の2倍で計算（v4-pro / v4-flash）。各メッセージの送信時刻をUTCで判定するため端末のタイムゾーンに依存しません |
| **2026-06-20** | **Sakana AI（fugu）に対応**：プロバイダーに「Sakana (fugu)」を追加（OpenAI互換）。モデル `fugu` / `fugu-ultra` を選択可。設定でAPIキーを入力すれば利用できます |
| **2026-06-19** | **DeepSeek の料金計算に対応**：会話統計(ⓘ)の推定コストで DeepSeek（reasoner / chat / v4-pro）の料金を計算（キャッシュヒット入力も考慮）。※価格は変動するため必要に応じて更新 |
| **2026-06-19** | **テキストファイル添付の不具合を修正**：DeepSeek 等のテキスト専用モデルにテキストファイルを添付すると `image_url` エラーで失敗していた問題を解消（添付は種類別に、画像＝画像／テキスト等＝本文テキストとして送信） |
| **2026-06-19** | **Dropbox 同期の安定化**：一時的なサーバーエラー（503/429等）を自動リトライ（指数バックオフ）。Service Worker が Dropbox 通信に干渉しないよう除外 |
| **2026-06-16** | **「思考の深さ」OFF で推論を完全停止**：Anthropic で Effort を OFF にしても思考の予算が残っていると推論が止まらなかった問題を修正 |
| **2026-06-16** | メッセージ操作から個別「画像保存」ボタンを削除（💾・トークン表示の見切れを解消）。画像保存はフローティングパネルの「範囲を画像保存」で1件選択でも可能。あわせてラベルに送信時刻(HH:MM)を表示 |
| **2026-06-14** | **メッセージの範囲を画像保存**：フローティングパネルの「範囲を画像保存」から、開始〜終了メッセージを選んでまとめて1枚のPNGに保存できる機能を追加（長い範囲は自動で縮小、極端に長い場合は複数枚に分割） |
| **2026-06-14** | **iPhoneで画像保存が失敗する問題を修正**：iOS Safariで「The operation is insecure」エラーや、本文が白く/薄くなる・思考プロセスが漏れる・行が重なる不具合を解消（画像生成を html2canvas-pro に変更し、撮影時のみ高コントラスト配色を適用して確実に黒文字で出力） |
| **2026-06-14** | DeepSeek 等（OpenAI互換プロバイダー）でチャットの自動タイトルが生成されない問題を修正（推論モデル利用時も軽量モデルでタイトルを生成） |
| **2026-06-14** | セキュリティ強化：AI応答・インポートしたログのHTMLサニタイズを多層防御化（DOMPurify を追加で適用） |
| **2026-06-11** | **テキストアーティファクト機能を追加**：AI応答内のコードブロック（```で囲まれた部分）を、言語ラベルとコピーボタン付きのカードで表示。プロンプトや長文をワンタップでコピー可能に |
| **2026-06-11** | **【重要】セキュリティ修正（XSS）**：AI応答やインポートしたログ内に含まれる生HTML（`<img onerror=...>`等）がそのまま実行され得る脆弱性を修正。生HTMLはエスケープ表示に、`javascript:`等の危険なリンクは無効化。**他人から受け取ったログをインポートする場合があるユーザーは必ず更新してください**（APIキー・Dropboxトークンが盗まれる恐れがありました） |
| **2026-06-11** | 会話履歴キャッシュを復活：一時的に無効化され履歴全体が毎ターン通常入力で課金される状態になっていたのを修正。TTLは設定値（5分/1時間）に従います |
| **2026-06-11** | 長期記憶の自動学習間隔に「75」「100メッセージごと」を追加 |
| **2026-06-02** | **Anthropicプロンプトキャッシュ改善**：トップレベル自動キャッシュ方式に変更し、キャッシュポイントが会話の伸びに合わせて自動前進。5分/1時間キャッシュ書き込みを区別したコスト計算に対応。料金テーブルを現行価格に更新（Opus 4.5〜4.8: $5/$25/M、Haiku 4.5: $1/$5/M） |
| **2026-05-30** | **プロジェクトSPの巻き戻りバグを修正**：プロジェクト管理でシステムプロンプトを編集後にDropbox同期すると古い内容に戻る問題を修正（`updatedAt`未更新によるマージ競合負け） |
| **2026-05-30** | Dropbox自動同期（競合マージ・自動Pull）後のページ全体リロードをソフトリロードに変更。画面が一瞬白紙になってチャット履歴が消えて見える問題を解消 |
| **2026-05-30** | Anthropicタイトル自動生成バグを修正：thinkingモデル使用時にレスポンスの`content[0]`がthinkingブロックになりタイトルが生成されなかった問題を修正 |
| **2026-05-30** | ダミープロンプトのオン／オフトグルを追加（設定 → アドバンスド） |
| **2026-05-23** | **Anthropic会話履歴キャッシュを修正**：毎ターン全会話履歴が通常入力として課金されていたバグを修正。最後のアシスタントメッセージに`cache_control`を付与することで、会話履歴を設定TTL（1h等）でキャッシュするよう変更。大幅なコスト削減効果あり |
| **2026-05-23** | Anthropic推定コストの料金テーブルを実際の価格に修正（Claude 4系: Opus $5/$25/M、Sonnet $3/$15/M、Haiku $0.8/$4/M） |
| **2026-05-23** | Anthropicキャッシュ1時間TTLが正しく機能するよう修正（`ttl: "1h"` の文字列形式。以前は文字列形式でもAPIに無視されていた） |
| **2026-05-23** | Anthropicツール定義への`cache_control`を廃止：毎ターン1,000トークン相当の無駄なキャッシュ書き込みが発生していた問題を解消 |
| **2026-05-18** | フローティングパネルに会話統計ダイアログを追加（ⓘボタン）：メッセージ数・トークン内訳・キャッシュヒット率・推定コスト・会話サイズを表示 |
| **2026-05-16** | モデル選択がリロード後に初期値に戻るバグを修正 |
| **2026-05-08** | Anthropicのキャッシュトークン内訳（💾読み込み / ✏️書き込み）をメッセージ下部に表示 |
| **2026-04-27** | アプリ名を「Aquarium Chat」に改名。チャットタイトル自動生成追加。Dropbox同期を競合自動マージに変更。iOS Safariズームバグ修正 |
| **2026-04-26** | プロバイダー別に最後に使ったモデルを記憶。API取得モデルがリロード後に消えるバグ修正。プロジェクト選択をリロード後も保持 |
| **2026-04-22** | モデルの応答にターン番号とモデル名を表示。プロンプトキャッシュに「なし」オプション追加 |
| **2026-04-17** | **Claude Adaptive Thinking対応**：思考の深さ（effort: low/medium/high/max）を設定画面から選択可能に |
| **2026-04-13** | **NovelAI画像生成対応**：「絵を描いて」と言うとアニメ・イラスト調の画像を自動生成。全プロバイダー対応 |
| **2026-04-13** | OpenAI・Claude プロバイダーに Function Calling サポートを追加 |
| **2026-04-11** | **Anthropic プロンプトキャッシング対応**：静的部分（システムプロンプト・ナレッジ）のみキャッシュ対象とし、動的部分（記憶・サマリー）はキャッシュ対象外に分離 |
| **2026-04-07** | プロジェクト・ナレッジファイルをDropbox同期対象に追加。ナレッジファイルの直接編集機能を追加 |
| **2026-04-07** | メッセージ本文をコピーするボタンを追加 |
| **2026-03-31** | プロバイダー別モデル表示。全プロバイダー最新モデル自動取得ボタン追加 |
| **2026-03-29** | プロジェクトナレッジ機能：ファイルをプロジェクトに添付し全チャットで自動読み込み |
| **2026-03-28** | 思考プロセス表示対応：Gemini 3系・Claude Extended Thinking・DeepSeek-R1 |

---

## フォーク元バージョン履歴

| バージョン | 主な変更 |
|---|---|
| **カスタム版ベース** | OpenAI・Anthropic・Groq・DeepSeek・xAI・Mistral のネイティブサポート、推論モデル対応 |
| **v1.0** | Dropbox連携、Stable Diffusion連携（本カスタム版では削除） |
| **v0.5** | コンテキスト圧縮、キャラクターメモリ、メモリ機能、キャラクタープロファイル画面 |
| **v0.4** | マルチモーダル対応、画像生成/編集/動画生成、プロファイル機能、画像アセット管理 |
| **v0.3以前** | 基本チャット機能、Function Calling、自動リトライ、校正機能 |

